<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wow.itc.com.wow_itc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PERegisterActivity" />
        <activity android:name=".HouseCheck" />
        <activity android:name=".FirstTime" />
        <activity android:name=".FollowUpPropagationActivity" />
        <activity android:name=".HubCollectionRegister" />
        <activity android:name=".HubCollectionActivity"></activity>
    </application>

</manifest>